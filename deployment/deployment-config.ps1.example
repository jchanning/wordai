# Deployment Configuration
# Copy this file to deployment-config.ps1 and update with your values
# DO NOT commit deployment-config.ps1 to git (it's in .gitignore)

# Oracle Cloud Instance IP Address
$IP = "YOUR_SERVER_IP_HERE"

# SSH Key Path (absolute path)
$KEY = "C:\Users\YOUR_USERNAME\.ssh\arm-wordai.key"
