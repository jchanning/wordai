# Deprecated Code Removal Plan

Status note (2026-04-25): the `ARCH-20` cleanup removed the remaining live legacy entropy-calculation path from `WordEntropy` and left `ResponseMatrix` as the authoritative entropy implementation. The inventory below is retained as historical planning context and no longer reflects the current code line-for-line.

_Generated by static analysis of the codebase. All deprecated items are located in `WordEntropy.java` and its supporting classes. No deprecated Spring Security patterns were found — `SecurityConfig` already uses the modern Lambda DSL._

---

## Executive Summary

The project contains a **dual-path architecture** in `WordEntropy.java` originating from a Phase 1 optimisation that replaced a `ConcurrentHashMap`-based response cache with a dense `ResponseMatrix`. The migration was 80% complete: the new matrix path (`useMatrix = true`) was hard-wired and the pre-computation path was fully updated, but **the legacy path was never deleted**. As a result:

- A `private final boolean useMatrix = true` constant creates permanently dead `else` branches.
- A `static ConcurrentHashMap<WordPairKey, Short> responseCache` is still used at runtime by `getResponseBuckets()`, called by `SelectBellmanFullDictionary` and the lazy-entropy path of `getEntropy()`.
- Eleven distinct deprecated items span three categories: **dead code** (safe to delete), **orphan classes** (delete after callers removed), and **live legacy code requiring migration** (must migrate before removal).

No deprecated stdlib or framework APIs were detected outside `WordEntropy.java` and its direct dependencies.

---

## Deprecated Item Inventory

### DEP-01 — `useMatrix` dead-code constant and its branches

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java` — field at line 61; dead branches at lines 180–196, and inside constructors |
| **Type** | Dead code constant |
| **Risk** | None — `useMatrix = true` is always `true`; `else` / `if (!useMatrix)` blocks are never executed |
| **External callers** | None |

**Action:** Remove the `private final boolean useMatrix = true` field. For every `if (useMatrix) { A } else { B }` block, replace the entire construct with just `A`. For every standalone `if (!useMatrix) { ... }` block, delete it entirely.

---

### DEP-02 — `getResponseCacheSize()` static method

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:92` |
| **Type** | `@Deprecated` public static method |
| **External callers** | None found in codebase |
| **Annotation** | `@Deprecated` with Javadoc "Use `getResponseMatrix().getEstimatedMemoryBytes()` instead" |

**Action:** Delete method. No callers to update.

---

### DEP-03 — `clearResponseCache()` static method

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:104` |
| **Type** | `@Deprecated` public static method |
| **External callers** | None found in codebase |

**Action:** Delete method. No callers to update.

---

### DEP-04 — `getEstimatedCacheMemoryBytes()` static method

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:132` |
| **Type** | Not `@Deprecated` but delegates entirely to `responseCache.size()` — always returns `0` since `responseCache` is never populated in the current active path |
| **External callers** | None found in codebase |

**Action:** Delete method. If a cache-size metric is needed, use `getResponseMatrix().getEstimatedMemoryBytes()` which is already available.

---

### DEP-05 — `precomputeWithCache()` private method

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:284` |
| **Type** | `@deprecated` (Javadoc tag) private legacy precomputation path |
| **Callers** | Only from the dead `if (!useMatrix)` branch in the constructor (eliminated by DEP-01) |

**Action:** Delete method. This becomes unreachable once DEP-01 is applied. Contains its own call to `getResponseBuckets()` and the three legacy `calculateXxxFromBuckets()` helpers (DEP-09).

---

### DEP-06 — `getResponseBucketsFromMatrix()` private method

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:307` |
| **Type** | `@Deprecated @SuppressWarnings("unused")` — already unused |
| **Callers** | None — the comment says "backward compatibility" but no call site exists |

**Note:** This method contains the correct `ResponseMatrix`-based implementation of bucket grouping. Inline its logic into the replacement `computeResponseBuckets()` as part of DEP-08 before deleting it.

**Action:** Do **not** delete in isolation — inline its logic into DEP-08's migration, then delete.

---

### DEP-07 — `responseCache` static field + `computeResponseBuckets()` private method _(live, migration required)_

| Field | Value |
|---|---|
| **Location** | `responseCache` at `WordEntropy.java:81`; `computeResponseBuckets()` at line 368 |
| **Type** | `@Deprecated` static `ConcurrentHashMap<WordPairKey, Short>`; private method that populates it at runtime |
| **Runtime status** | **Currently active.** `responseCache` is populated every time `computeResponseBuckets()` runs |
| **Called by** | `getResponseBuckets()` (see DEP-08) |

**Prerequisite:** DEP-08 must migrate `getResponseBuckets()` away from `computeResponseBuckets()` first. Once `computeResponseBuckets()` has no callers, both it and `responseCache` can be deleted together.

**Action (after DEP-08):** Delete `computeResponseBuckets()` method and `responseCache` field.

---

### DEP-08 — `getResponseBuckets(String)` public method _(live, migration required)_

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java:353` |
| **Type** | Public method, not annotated `@Deprecated`, but its only implementation (`computeResponseBuckets`) uses the deprecated `responseCache` + `WordPairKey` |
| **External callers** | `SelectBellmanFullDictionary.java:85` (sole external caller) |
| **Internal callers** | `precomputeWithCache()` (DEP-05, dead) and `calculateEntropy()` → `getEntropy()` (live, lazy path) |

This is the **critical migration item** that gates all downstream removals.

**Migration plan:**

1. **Update `computeResponseBuckets(String guessWord)`** — replace the `responseCache`/`WordPairKey` implementation with the `ResponseMatrix`-based logic from DEP-06's `getResponseBucketsFromMatrix()`:
   ```java
   private Map<Short, Set<String>> computeResponseBuckets(String guessWord) {
       int guessId = responseMatrix.getWordId(guessWord);
       Map<Short, Set<String>> result = new HashMap<>();
       int wordCount = responseMatrix.getWordCount();
       for (int targetId = 0; targetId < wordCount; targetId++) {
           short pattern = responseMatrix.getPattern(guessId, targetId);
           String targetWord = responseMatrix.getWord(targetId);
           result.computeIfAbsent(pattern, k -> new HashSet<>()).add(targetWord);
       }
       return result;
   }
   ```
2. The `getResponseBuckets(String)` public signature is **retained** — `SelectBellmanFullDictionary` continues to call it unchanged, but it now uses the matrix path internally.

**Action:** Migrate `computeResponseBuckets()` to use `ResponseMatrix` as above. No changes required to callers.

---

### DEP-09 — Legacy bucket-based helper methods

| Field | Value |
|---|---|
| **Location** | `WordEntropy.java` lines ≈ 723–820 |
| **Methods** | `calculateEntropyFromBuckets()`, `calculateDictionaryReductionFromBuckets()`, `calculateColumnLengthFromBuckets()`, `calculateColumnLengthForWords()` |
| **Called by** | `precomputeWithCache()` (DEP-05, dead) AND `calculateEntropy()` via `getResponseBuckets()` (live lazy path) |

Once DEP-08 is complete, `getResponseBuckets()` returns matrix-derived buckets. These helper methods continue to work correctly against any `Map<Short, Set<String>>` argument — they have no dependency on the old cache internals. Therefore they can be retained or removed opportunistically:

- `calculateEntropyFromBuckets()` — called by `calculateEntropy()` (live). Safe to keep.
- `calculateDictionaryReductionFromBuckets()` — called by `precomputeWithCache()` only (dead after DEP-05). Can be deleted.
- `calculateColumnLengthFromBuckets()` — called by `precomputeWithCache()` only (dead after DEP-05). Can be deleted.
- `calculateColumnLengthForWords()` — called by `calculateColumnLengthFromBuckets()`. Can be deleted.

**Action:** After DEP-05 is applied, delete `calculateDictionaryReductionFromBuckets()`, `calculateColumnLengthFromBuckets()`, and `calculateColumnLengthForWords()`. Retain `calculateEntropyFromBuckets()` as it is still called by the live `calculateEntropy()` path.

---

### DEP-10 — `WordPairKey` class

| Field | Value |
|---|---|
| **Location** | `src/main/java/com/fistraltech/core/WordPairKey.java` |
| **Type** | Whole class — composite map key for the deprecated `responseCache` |
| **Callers** | `computeResponseBuckets()` (DEP-07, live until DEP-08 migration) and `ResponseCacheTest.java` |

**Prerequisite:** DEP-07 must be completed first (removal of `computeResponseBuckets()` after DEP-08 migration).

**Action:** Delete `WordPairKey.java`. Remove all `WordPairKey`-related tests from `ResponseCacheTest.java` (or delete the test class entirely — see DEP-11).

---

### DEP-11 — `ResponsePattern` class

| Field | Value |
|---|---|
| **Location** | `src/main/java/com/fistraltech/core/ResponsePattern.java` |
| **Type** | Utility class for `short`-encoding of response patterns |
| **Callers** | `ResponseCacheTest.java` only; referenced only in `WordEntropy` Javadoc comments |

`ResponsePattern` is a well-implemented utility. Its encoding logic is duplicated inline in `WordEntropy.encodeResponse()` and `WordEntropy.decodeResponse()`. `ResponseMatrix` encodes patterns directly as `short` values using the same scheme but without going through this class.

There are two valid options:
- **Keep**: Retain as a documented, tested utility class. Update `ResponseCacheTest` to test properties independent of the deprecated cache.
- **Remove**: Delete the class; update `ResponseCacheTest`; replace `encodeResponse()`/`decodeResponse()` in `WordEntropy` with a static utility or inline logic. Remove the Javadoc `@see` references in `WordEntropy`.

**Action (recommended):** Keep `ResponsePattern.java` — it is a clean, well-documented class with independent value as a utility. Delete `ResponseCacheTest.java` and replace it with a focused `ResponsePatternTest.java` that tests encode/decode invariants directly.

---

### DEP-12 — `ResponseCacheTest.java` test class

| Field | Value |
|---|---|
| **Location** | `src/test/java/com/fistraltech/analysis/ResponseCacheTest.java` |
| **Type** | Test class for deprecated cache infrastructure (`WordPairKey`, `ResponsePattern`) |
| **Impact** | Must be updated or replaced when DEP-10 removes `WordPairKey` |

**Action:** After DEP-10, delete `ResponseCacheTest.java`. Create `ResponsePatternTest.java` in the same package to cover `ResponsePattern` encode/decode invariants (these tests currently exist in `ResponseCacheTest` but are mixed with now-obsolete cache tests).

---

## Execution Order

The items form a dependency chain. Follow this order to avoid breaking the build at each step:

### Phase 1 — Dead code elimination (no migration required)

| Step | Items | Notes |
|---|---|---|
| 1a | DEP-01 | Remove `useMatrix` field and inline/delete all dead branches |
| 1b | DEP-05 | Delete `precomputeWithCache()` (now unreachable after 1a) |
| 1c | DEP-09 (partial) | Delete `calculateDictionaryReductionFromBuckets()`, `calculateColumnLengthFromBuckets()`, `calculateColumnLengthForWords()` (now only called from DEP-05 which is gone) |
| 1d | DEP-02, DEP-03, DEP-04 | Delete `getResponseCacheSize()`, `clearResponseCache()`, `getEstimatedCacheMemoryBytes()` |

Run `mvn test` to confirm all 289 tests pass before proceeding.

### Phase 2 — Migration (converts live legacy path to matrix path)

| Step | Items | Notes |
|---|---|---|
| 2a | DEP-08 | Replace `computeResponseBuckets()` internals with `ResponseMatrix` lookup; inline DEP-06 logic and delete DEP-06 (`getResponseBucketsFromMatrix()`) |
| 2b | DEP-07 | Delete `computeResponseBuckets()` (now replaced) and `responseCache` field |

Run `mvn test` after each step.

### Phase 3 — Orphan cleanup

| Step | Items | Notes |
|---|---|---|
| 3a | DEP-10 | Delete `WordPairKey.java` |
| 3b | DEP-12 | Delete `ResponseCacheTest.java`; create `ResponsePatternTest.java` |
| 3c | DEP-11 | (Optional) Rename/reorganise `ResponsePattern` if desired |

Run `mvn test` to verify final state.

---

## Files Affected

| File | Change |
|---|---|
| `src/main/java/com/fistraltech/core/WordEntropy.java` | Major surgery: remove 11 methods/fields across Phases 1–2 |
| `src/main/java/com/fistraltech/bot/selection/SelectBellmanFullDictionary.java` | No change — `getResponseBuckets()` signature is retained |
| `src/main/java/com/fistraltech/core/WordPairKey.java` | **Delete** in Phase 3 |
| `src/main/java/com/fistraltech/core/ResponsePattern.java` | No change (retained as utility) |
| `src/test/java/com/fistraltech/analysis/ResponseCacheTest.java` | **Delete** in Phase 3; replace with `ResponsePatternTest.java` |

---

## Risk Assessment

| Item | Risk | Notes |
|---|---|---|
| DEP-01 through DEP-06 | Very low | Dead code; no callers outside the dead branch |
| DEP-08 (migration) | Medium | Changes runtime behaviour of `getResponseBuckets()`; must be covered by existing tests |
| DEP-07/09 (cleanup) | Low | Remove after DEP-08 confirms tests pass |
| DEP-10/12 (class deletion) | Low | `WordPairKey` only used by the deleted `computeResponseBuckets()`; test class updated |

The existing `SelectBellmanFullDictionaryTest` and `WordEntropyLazyTest` suites provide regression coverage for the DEP-08 migration.
